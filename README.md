# Reflection
This is a camera-based reflection module made for a web in which the device camera is used to map a projection on the provided object. For this test I am providing a silver circle as a subject body on which the camera image is projected with exact curve following with shadow mapping and all.
The complete code is written in HTML, compiled in a single file, code by AI 
